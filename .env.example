# 백엔드 API base URL.
# - 개발: 비워두면(권장) Vite 프록시(/api → VITE_DEV_API_TARGET)를 타서 CORS 불필요
# - 배포: 실제 API 오리진 지정 (예: https://api.seed.example)
VITE_API_BASE_URL=

# 개발 서버 프록시가 /api 요청을 전달할 백엔드 주소 (기본 http://localhost:8080)
VITE_DEV_API_TARGET=http://localhost:8080
